/**
 * Represents the content area of the module.
 *
 * ### How to use:
 * Place this directly within the module view, and place the content within
 * `header`, `section` and `footer` elements, which provide margin.
 */
export class ModuleContentCustomElement
{
}
