/**
 * Represents the sidebar area of the module.
 *
 * ### How to use:
 * Place this directly within the module view, either before or after the
 * `module-content` element, and place the content of the sidebar within
 * `header`, `section` and `footer` elements, which provide margin.
 */
export class ModuleSidebarCustomElement
{
}
