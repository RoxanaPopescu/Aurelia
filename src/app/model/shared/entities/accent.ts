/**
 * Represent the accent to use for emphasise.
 * For example, the accent of a status value might be "positive" if it represents a good
 * state, indicating that the UI should present it using the "positive" accent color.
 */
export type Accent = "neutral" | "positive" | "attention" | "negative";
